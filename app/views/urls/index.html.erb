
<%= form_for @url do |f| %>
<%= f.label :long_url %>
<%= f.text_field :long_url %>
<%= f.submit %>
<% end %>

<% @urls.each do |url| %>
<%= link_to "/url/#{url.short_url}", short_url_path(url) %><br>
<% end %>
